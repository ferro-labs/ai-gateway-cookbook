# Ferro Labs AI Gateway endpoint. Defaults to a locally running gateway.
FERRO_BASE_URL=http://localhost:8080

# API key the SDK presents to the gateway.
FERRO_API_KEY=

# Override the default agent task (optional).
# AGENT_REQUEST=Build a CLI that fetches the current UTC time and prints it as ISO-8601.

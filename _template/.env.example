# Ferro Labs AI Gateway endpoint. Defaults to a locally running gateway.
FERRO_BASE_URL=http://localhost:8080

# API key the SDK presents to the gateway (the gateway maps it to provider credentials).
FERRO_API_KEY=
